pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // To be able to use MPAndroidChart
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Runner"
include ':app'
